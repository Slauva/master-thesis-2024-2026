\newpage
\begin{center}
  \textbf{\large ПРИЛОЖЕНИЕ А}
\end{center}
\refstepcounter{chapter}
\addcontentsline{toc}{chapter}{ПРИЛОЖЕНИЕ А}
\label{app:dataset}

\phantomsection
\section*{Структура используемого набора данных}
\addcontentsline{toc}{section}{Структура используемого набора данных}

Используемый набор данных содержит две группы записей, соответствующие различным фазам экспериментального протокола. Первая группа относится к этапу предъявления и запоминания стимулов, вторая - к этапу воспоминания и визуального воображения. В основной задаче реконструкции используются записи второй группы, поскольку именно они соответствуют EEG-сигналам, зарегистрированным во время мысленного воспроизведения изображения.

Данные организованы иерархически: верхний уровень соответствует испытуемому, следующий уровень - отдельной экспериментальной пробе. Внутри каждой пробы хранятся EEG- и EOG-записи в формате \texttt{.fif}, а также файл разметки \texttt{labels.json}. EEG-файлы содержат многоканальные сигналы электроэнцефалографии, EOG-файлы - данные, связанные с движениями глаз и контролем взгляда.

Файл \texttt{labels.json} содержит объект с ключом \texttt{blocks}. Значение этого ключа представляет собой список блоков, каждый из которых описывает один целевой фрагмент эксперимента. Используемые поля разметки приведены в табл. \ref{tab:labels-json-format}.

\begin{table}[!t]
  \centering
  \caption{Основные поля файла разметки \texttt{labels.json}.}
  \label{tab:labels-json-format}
  \begin{tabular}{|p{45mm}|p{95mm}|}
    \hline
    Поле & Содержание \\
    \hline
    \texttt{blocks} & Список целевых блоков внутри экспериментальной пробы. \\
    \hline
    \texttt{Exec\_Block\_Index} & Порядковый номер execution-блока, с которым связаны EEG- и EOG-записи. \\
    \hline
    \texttt{type} & Тип стимула: геометрический паттерн или случайный паттерн. \\
    \hline
    \texttt{pattern\_id} & Идентификатор паттерна внутри набора стимулов. \\
    \hline
    \texttt{img} & Бинарная матрица размера $6 \times 6$, где значения 0 и 1 задают структуру целевого изображения. \\
    \hline
  \end{tabular}
\end{table}

Таким образом, каждая EEG-эпоха связывается с целевым изображением через индекс execution-блока и соответствующую запись в \texttt{labels.json}. Перед обучением моделей матрица \texttt{img} векторизуется в последовательность из 36 бинарных значений, что позволяет рассматривать реконструкцию как многовыходную бинарную задачу.

\phantomsection
\section*{Формулы метрик оценки реконструкции}
\addcontentsline{toc}{section}{Формулы метрик оценки реконструкции}
\label{app:metrics}

Пусть для одного примера истинное изображение и реконструкция представлены бинарными векторами $\mathbf{y}, \hat{\mathbf{y}} \in \{0,1\}^{m}$, где $m = 36$. Для вычисления метрик используются следующие величины:

\begin{align}
TP &= \sum_{j=1}^{m} \mathbb{I}(y_j = 1 \land \hat{y}_j = 1), \\
TN &= \sum_{j=1}^{m} \mathbb{I}(y_j = 0 \land \hat{y}_j = 0), \\
FP &= \sum_{j=1}^{m} \mathbb{I}(y_j = 0 \land \hat{y}_j = 1), \\
FN &= \sum_{j=1}^{m} \mathbb{I}(y_j = 1 \land \hat{y}_j = 0).
\end{align}

Здесь $TP$ обозначает число верно восстановленных активных пикселей, $TN$ - число верно восстановленных неактивных пикселей, $FP$ - число ложноположительных активных пикселей, а $FN$ - число пропущенных активных пикселей.

Pixel Accuracy определяется как доля правильно восстановленных пикселей:

\begin{equation}
\operatorname{PixelAccuracy} =
\frac{TP + TN}{TP + TN + FP + FN}.
\end{equation}

Precision, recall и F1-score относятся к стандартным метрикам бинарной классификации~\cite{10.1016/j.ipm.2009.03.002}:

\begin{align}
\operatorname{Precision} &= \frac{TP}{TP + FP}, \\
\operatorname{Recall} &= \frac{TP}{TP + FN}, \\
\operatorname{F1} &= 2 \cdot
\frac{\operatorname{Precision} \cdot \operatorname{Recall}}
{\operatorname{Precision} + \operatorname{Recall}}.
\end{align}

Метрика Intersection over Union (IoU), также связанная с Jaccard overlap, оценивает совпадение активных областей истинного и восстановленного изображения~\cite{10.1186/s12880-015-0068-x}:

\begin{equation}
\operatorname{IoU} =
\frac{TP}{TP + FP + FN}.
\end{equation}

Hamming loss показывает долю несовпадающих пикселей между истинным и восстановленным бинарным вектором. Эта метрика основана на идее расстояния Хэмминга~\cite{10.1002/j.1538-7305.1950.tb00463.x}:

\begin{equation}
\operatorname{HammingLoss} =
\frac{1}{m}\sum_{j=1}^{m}\mathbb{I}(y_j \ne \hat{y}_j).
\end{equation}

При выраженном дисбалансе активных и неактивных пикселей дополнительно может использоваться balanced accuracy:

\begin{equation}
\operatorname{BalancedAccuracy} =
\frac{1}{2}
\left(
\frac{TP}{TP + FN} +
\frac{TN}{TN + FP}
\right).
\end{equation}

Exact match accuracy является наиболее строгой метрикой: реконструкция считается правильной только в том случае, если совпали все 36 пикселей:

\begin{equation}
\operatorname{ExactMatch} =
\mathbb{I}(\mathbf{y} = \hat{\mathbf{y}}).
\end{equation}

Для набора данных указанные метрики усредняются по всем примерам тестовой выборки. В данной работе exact match accuracy рассматривается только как вспомогательная метрика, поскольку для малых EEG-датасетов и изображения из 36 пикселей она может быть чрезмерно жёсткой.
